  <% if signed_in? && current_user.admin? %>
  <%= link_to 'New Product', new_product_path, class: 'pull-right btn btn-primary' %>
<% end %>

<!-- This is the Products Page -->

<div class="container">
  <h1>Products</h1>
  <% cache(cache_key_for_products) do %> <!-- Start of Cache -->
    <%= render @products, class: 'img-responsive' %>
  <% end %><!-- /End of Cache -->
</div>

  <%= will_paginate @products %><br>
