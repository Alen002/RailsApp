<%= form_with(model: first_name, local: true) do |form| %>
  <% if first_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(first_name.errors.count, "error") %> prohibited this first_name from being saved:</h2>

      <ul>
      <% first_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :first_name_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
